# Gulp | Sass | Browser-Sync | Pug | Babel Boilerplate

## Clone the git repo
 ```
 git clone 
 ```

 ## Change directory to gulp-front-end

 ```
 cd gulp-front-end
 ```

 ## Install dependancy packages

 ```
 npm install 
 ```
 or
 ```
 npm i
 ```
 ## Start the gulp default task

 ```
 gulp
 ```

